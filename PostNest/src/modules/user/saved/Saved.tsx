import { SavedWrapper } from "@/modules/user/saved/style";

const Saved = () => {
    return (
        <SavedWrapper>
            {
                //TODO добавить посты
            }
        </SavedWrapper>
    );
};

export default Saved;